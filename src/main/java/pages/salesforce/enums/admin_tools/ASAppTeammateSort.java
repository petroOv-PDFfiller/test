package pages.salesforce.enums.admin_tools;

public class ASAppTeammateSort {

    public enum SortBy {
        NAME, EMAIL, PROFILE, STATUS
    }

    public enum OrderBy {
        ASCENDING, DESCENDING
    }
}